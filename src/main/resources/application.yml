---
spring:
  datasource:
    url: jdbc:h2:mem
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  jpa:
    hibernate.ddl-auto: create-drop
    show_sql: false
  profiles:
    active: mock
security:
  basic:
    enabled: false